<% if list.errors.any? %>
  <div class="alert alert-warning" id="error_explanation">
    <p>
      <%= pluralize(list.errors.count, "error") %>
      prohibited this list from being saved:
    </p>
 
    <ul>
    <% list.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for list do |f| %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>
  </p>
  <p>
    <%= f.label "collaborators", "Collaborators (add emails separated by commas or spaces)"%><br>
    <%= f.text_area "collaborators", value: list.collaborators %> 
  </p>

  <%= f.submit class: "btn btn-primary"%>
<% end %>
